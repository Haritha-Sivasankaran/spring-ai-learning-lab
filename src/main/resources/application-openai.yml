spring:
  ai:
    model:
      chat: openai
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        model: ${OPENAI_CHAT_MODEL:gpt-5-mini}
        temperature: ${OPENAI_CHAT_TEMPERATURE:0.4}
